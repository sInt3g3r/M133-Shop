export type BuyProduct = {
    id: string,
    amount: number,
    price: number
}

export type Product = {
    id: string,
    productName: string,
    specialOffer: number,
    normalPrice: number,
    imageName: string,
    description: string
  }